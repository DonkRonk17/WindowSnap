# WindowSnap - Requirements
# Smart Window Layout Manager
psutil>=5.9.0

# Platform-Specific (install as needed):
# Windows: pip install pywin32
# macOS: pip install pyobjc-framework-Quartz
# Linux: sudo apt install wmctrl
# GUI: pip install PyQt5
